# Projection

This lib has moved to the @extref:[Apache Pekko Projection](pekko-projection:). This was part of the 2.0.0 refactor. The pekko-projection-r2dbc lib still works the same way but it will be released at the same time as other Pekko Projection libs instead of being released with pekko-persistence-r2dbc.
